from setuptools import setup, find_packages

setup(
    name="multittpp",
    version="0.1.0",
    description="Learning multivariate temporal point processes via the time-change theorem",
    packages=find_packages("."),
    zip_safe=False,
)
